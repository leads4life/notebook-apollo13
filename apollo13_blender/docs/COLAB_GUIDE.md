# Colab guide
Open `colab/Apollo13_Blender_Render.ipynb` and execute cells in displayed order. Four manual approval booleans and the final-quality approval deliberately default to `False`; the final cell raises `FINAL RENDER BLOCKED` until all are set to `True`. Persist the project/frames under Drive to resume disconnected runs.
